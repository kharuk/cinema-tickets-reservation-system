class SearchFilmServices{
    
  filterFilmListByDate = (filmList, startDate, endDate) => {
    let filtredFilmList = filmList;
    
  }
}

const searchFilmService = new SearchFilmServices();

export default searchFilmService;